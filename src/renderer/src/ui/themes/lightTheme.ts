import { createTheme } from "@mui/material";

const lightTheme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#00B2FF",
    },
    secondary: {
      main: "#ffffff",
    },
  },
});

export default lightTheme;
